import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.workhourstracker.app',
  appName: 'Work Hours Tracker',
  webDir: 'dist'
};

export default config;
